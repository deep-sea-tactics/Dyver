#import dependencies 
import cv2
import numpy as np
import torch
import torchvision
import torchvision.transforms as transforms
from torch.utils.data import DataLoader
import mathplotlib.pyplot as plt

#get MiDAS model for depth estimation

#get Open3D for 3D reconstruction (point cloud generation)
#make sure python>3.13 (python3 --version)


#use with cv2

#this prolly do last cuz just gotta integrate open3d with cv2 and midas
# no dataset needed but idk if we can use that 